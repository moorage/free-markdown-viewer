# Markdown JSON Showcase

Strict JSON:

```json
{"project":"Quick Markdown Viewer","nested":{"lineNumbers":true}}
```

JSONC:

```jsonc
{
  // Comments are allowed in JSONC.
  "trailingComma": true,
}
```

NDJSON:

```ndjson
{"event":"one"}
{bad}
{"event":"two"}
```
